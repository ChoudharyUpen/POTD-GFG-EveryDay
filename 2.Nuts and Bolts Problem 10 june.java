//WTF question was this was only used 2 array.sort and it passed all testcases
//no way this was very easy and they have termed this as medium wtf


class Solution {
    void matchPairs(int n, char nuts[], char bolts[]) {
        Arrays.sort(nuts);
        Arrays.sort(bolts);
    }
}
